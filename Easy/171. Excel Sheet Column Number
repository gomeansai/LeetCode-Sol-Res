public class Solution {
    public int titleToNumber(String s) {
        int result = 0;
        for (int n = 0; n < s.length(); n++) {
            result = result * 26 + (s.charAt(n) - 'A' + 1);
        }
        return result;
    }
}
